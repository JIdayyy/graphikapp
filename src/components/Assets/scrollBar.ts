const scrollBar = {
    "&::-webkit-scrollbar": {
        width: "3px",
    },
    "&::-webkit-scrollbar-thumb": {
        borderRadius: "20px",
        backgroundColor: "red.default",
    },
};

export default scrollBar;
