const scrollBar = {
    "&::-webkit-scrollbar": {
        width: "3px",
    },
    "&::-webkit-scrollbar-thumb": {
        borderRadius: "20px",
        backgroundColor: "purple.default",
    },
};

export default scrollBar;
